// sections
import { BudgetingView } from 'src/sections/budgeting/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Budget - To Save More Each Year',
};

export default function BudgetingPage() {
  return <BudgetingView />;
}